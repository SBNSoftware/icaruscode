This directory holds old configuration files that are unmaintained
but have been deemed useful to have as a model.
They are kept in `icaruscode` repository but they are not distributed in the releases.

