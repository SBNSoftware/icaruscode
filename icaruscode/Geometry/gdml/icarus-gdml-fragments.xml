<?xml version='1.0'?>

<!-- Input to Geometry/gdml/make_gdml.pl; define the GDML fragments
     that will be zipped together to create a detector description. 
     -->

<config>

   <constantfiles>

      <!-- These files contain GDML <constant></constant>
           blocks. They are read in separately, so they can be
           interpreted into the remaining GDML. See make_gdml.pl for
           more information. 
	   -->
	   

   </constantfiles>

   <gdmlfiles>

      <!-- The GDML file fragments to be zipped together. -->

      <filename> icarus_Def.gdml </filename>
      <filename> icarus_Materials.gdml </filename>
      <filename> icarus_TPC.gdml </filename>
      <filename> icarus_pmt.gdml </filename>
      <filename> icarus_Cryostat.gdml </filename>
      <filename> icarus_DetEnclosure.gdml </filename>
      <filename> icarus_World.gdml </filename>

   </gdmlfiles>

</config>
